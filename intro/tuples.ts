const user: (number | string)[] =['saju', 1]
const user1: (number | string)[] =[3,'saju']
// in the upper no restriction but in case of api call we need the values in a specific order


let tUser : [string, number, boolean];

tUser = ["saju", 13, true]

type rgb= [number,number,number];

const white: rgb = [0,0,0];
white.push(1)
console.log(white);

export {}