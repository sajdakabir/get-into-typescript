// const AISLE = 0
// const MIDDLE = 1
// const WINDOW = 2

// if (seat == 0){

// }

// in this case enum is come to play

const enum SeatChoice {
    // in enum the deafault number start with 0 then ++ but you can change
    AISLE = 10,
    MIDDLE = 40,
    WINDOW = "window"
}

const skSeat = SeatChoice.MIDDLE